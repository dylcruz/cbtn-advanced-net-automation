router ospf {{ host.facts.ospf.process_id }}
 router-id {{ host.facts.ospf.rid }}
{% set networks = host.facts.ospf.networks %}
{% for n in networks %}
 network {{ n.net }} {{ n.mask }} area {{ n.area }}
{% endfor %}
